import React from 'react';
import './card.scss';

const Card = () => {
    return (
        <div className="card">
            <p className="card__title">Titre de la location</p>
        </div>
    );
};

export default Card;
